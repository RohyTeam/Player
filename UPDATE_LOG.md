# 更新日志

<h3><b>W.I.P.</b></h3>

<b>待制作</b>

- 对于平板和 PC/2in1 设备支持导入文件夹当作自动扫描的播放列表
- 对于导入后的文件，支持再为指定文件导入字幕文件（为方便管理，同时由于字幕文件普遍比较小，字幕文件会直接被复制到播放器目录，不会持久化）

<details>
  <summary>0.3.0</summary>

<b>新增功能</b>

- 章节/书签支持：对于有章节/书签的视频，进度条上会标识各个章节/书签的开头，同时可以通过顶栏右上角的按钮打开章节/书签列表，可以在列表中直接点击对应的章节/书签来直接跳转到其开头
- 对于导入到播放列表内的视频，可以导入外部字幕了（永久性保存），同时也可以删除已经导入的外部字幕
- 设置页添加了“UI 更新（预览版）选项”，开启后可以使用预览版的新版 UI。目前新版 UI 更新了以下部分控件的 UI：播放器中的顶栏和底栏。
- 设置页移除了“模糊效果”，替换为了“视觉风格“，点击后可以进入一个模拟的播放器页，在此页面中可以调整支持模糊背景的控件的模糊效果、顶栏与底栏的圆角大小。
- 添加了“关于”页面
- 添加了“常见问题”页面
- 手机/平板 用户双击屏幕是左右两侧可以用于加减进度 15s（设置中可关闭）
- 手机/平板 用户长按加速时会显示一个加速中的提示
- 手机/平板 用户上下滑动左右屏幕时可以分别调整亮度和音量
- 手机/平板 用户可以通过左右滑动屏幕调整进度条位置

<b>功能调整与优化</b>

- 播放列表不再能嵌套文件夹（已创建的嵌套文件夹不会被删除，但不能创建新的嵌套文件夹）
- 移除了“更少的覆盖”选项，现在默认都显示更少的覆盖
- 现在可以一次导入多个视频，一次性导入的上限为 500 个
- 对 PGS 字幕渲染流程进行优化，现在崩溃的概率更小了，同时导致软件冻结的概率也更小了，但仍有一定概率触发该问题

<b>问题修复</b>

- 视频播放完毕后显示的仍是“暂停”图标而不是“播放“图标
- 播控中心的按钮现在只会在视频完全准备好之后才可以使用
- 导入视频的流程调整到异步线程，将不会再堵塞线程导致软件崩溃

</details>

<details>
  <summary>0.2.0</summary>
<b>新增功能</b>

- 播放列表
  - **目前还未支持 PC**
  - 播放列表可以导入文件管理中的视频文件
  - 导入到播放列表中的视频可以永久性导入本地的字幕文件
  - 播放列表里可以嵌套创建文件夹
  - 可以修改播放列表中文件夹和视频文件的名称，但是不能重复
  - 导入播放列表后的视频可以从播放列表中删除
  - 如果删除播放列表中的文件夹，文件夹下所有的视频和文件夹都会被删除
- 手机/平板用户支持双击平面切换暂停和播放状态
- 倍速播放
- 手机/平板用户支持按住屏幕以 3 倍速加速播放
- 选择指定音轨
- 选择字幕
- 支持选择视频文件内置的**全部**字幕
- 支持打开本地字幕
- ASS (.ass | .ssa) 格式字幕支持
- PGS (.sup) 格式字幕支持
- SRT (.srt) 格式字幕支持

<b>问题修复</b>

- 平板使用播放器会胡乱旋转
- 无法正常后台播放
</details>

<details>
  <summary>0.1.0</summary>
<b>新增功能</b>

- 应用内打开文件播放
- 基础的播放功能：播放、暂停、后退 15 秒、前进 15 秒
- 从文件预览跳转至播放器播放
- 接入播控中心
- 解析视频的封面（虽然目前用户看不到）
</details>