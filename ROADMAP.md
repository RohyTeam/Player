# Roadmap
## 0.3.0 手机/平板体验完善
- ~~优化 PGS 字幕加载，防止阻塞线程~~
- ~~手机/平板 用户长按加速时会显示一个加速中的提示。~~
- ~~手机/平板 用户支持滑动屏幕调整进度条位置。~~
- ~~手机/平板 用户支持滑动屏幕调整亮度和音量条。~~
- ~~手机/平板 用户双击屏幕是左右两侧可以用于加减进度 15s（设置中可关闭）。~~
- ~~添加“关于”页面。~~
- ~~添加“常见问题”页面。~~
- ~~导入视频支持一次性导入多个视频，同时优化导入视频的代码实现异步不阻塞。~~
- ~~支持章节功能。~~

## 0.4.0 PC 体验完善
- 支持导入 WebDAV 服务器。
- 为 PC 用户支持基础播放列表功能。
- 为 平板/PC 用户支持直接导入文件夹进行扫描的功能。
- 支持字体管理功能。
- ~~支持自定义控件的模糊效果。~~
- 为 PC 底部栏添加两种显示模式：范围内显示（鼠标在底部栏范围内才显示）| 移动鼠标显示（鼠标只要移动就显示）。
- 播放列表中顶部显示一个地址栏，可以点击直接跳转回更高层级的文件夹页。例：播放列表A > 文件夹B > 文件夹C，可以点击”播放列表A“直接回到播放列表根目录。
- 播放列表管理的时候可以打开多选进行批量操作，如删除、移动。
- 支持为导入到播放列表内的视频导入外部字幕（可永久性使用）。
- 播放列表内的视频支持连续播放。
- 添加“上一个”和“下一个”按钮可以直接在播放列表内直接切换前后的视频。

## 0.5.0 UI 革新
- 对大部分 UI 进行一波重新设计与优化。
- 调整配色方案，是深色模式下的配色有正常的辨识度。
- 修改底部栏和顶部栏的 UI 效果，去除背景，将原本背景的效果直接应用于控件。
- 为缺失动画效果的控件添加动画效果。
- 添加搜索功能，允许搜索播放列表内的视频，结果显示的时候将以播放列表分块显示，支持正则、支持筛选文件夹和视频。
- 支持流转。

## 0.6.0 内核革新
- 替换 IJKPlayer 为 RohyPlayer（W.I.P.）。
- 支持硬件解码。
- 支持选择优先的解码器（硬件解码仅支持 ohcodec 解码，其他的可以选择优先使用 ffmpeg 解码还是 ohcodec 解码）。
- 支持 HDR Vivid。
- 支持 Audio Vivid。
- 支持调整音轨播放偏移量（延迟 0.5s，加快 0.5s）。
- 支持调整字幕播放偏移量（延迟 0.5s，加快 0.5s）。
- 支持导入额外的音轨。
- 视频滤镜功能。
- 音频均衡器功能。