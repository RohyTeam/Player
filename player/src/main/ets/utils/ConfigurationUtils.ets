import { ConfigurationConstant } from "@kit.AbilityKit";
import { ensureContext, ensureUIAbilityContext } from "./AbilityUtils";

export function setColorMode(context: UIContext | Context, colorMode: ThemeColorMode) {
  // todo: wait for api18
  // ensureUIAbilityContext(context).setColorMode(colorMode - 1)
}